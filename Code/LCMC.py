class latticeMC:

    def __init__(self, size, dim, angleSize, temperature)
    """Constructeur de la classe qui initialise :
    -la lattice à l'aide de la classe discreteLattice
    -la température
    -l'énergie ?"""

    def runMC(self)
    """Fonction qui lance une simulation Monte-Carlo"""

    def energie(self)
    """Fonction qui renvoie l'énergie de la configuration actuelle"""

    def orderParameter(self)
    """Fonction qui calcule la valeur du paramètre d'ordre"""

    def boltzmannWeight(self)
    """Fonction qui calcule le poid de Boltzmann d'une configuration"""

    def partitionFunction(self)
    """Fonction qui renvoie la fonction de partition"""
